{
  "name": "@donebond/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "donebond": "./dist/index.js"
  },
  "scripts": {
    "build": "pnpm --filter @donebond/shared build && pnpm --filter @donebond/evidence build && tsc -p tsconfig.json",
    "test": "pnpm build && node --test test/*.test.mjs",
    "typecheck": "pnpm --filter @donebond/shared build && pnpm --filter @donebond/evidence build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@donebond/evidence": "workspace:*",
    "@donebond/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
