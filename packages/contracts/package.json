{
  "name": "@donebond/contracts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "forge build",
    "format:check": "forge fmt --check",
    "test": "node --test test/scaffold.test.mjs && forge test",
    "test:contracts": "forge test"
  }
}
